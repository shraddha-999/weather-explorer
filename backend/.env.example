GCS_BUCKET_NAME=your-gcs-bucket-name
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend.netlify.app
# Local dev only: path to a service-account key with Storage Object Admin on the bucket.
# Not needed on Cloud Run if the service's runtime SA already has bucket access.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
